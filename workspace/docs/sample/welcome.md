# サンプルドキュメント（RAG検索の動作確認用）

このファイルは `search_docs` による意味検索（RAG）のデモ用です。

## 経費精算のルール（架空）
- 交通費は月末までに申請すること。
- 領収書は PDF で添付する。5万円以上は事前承認が必要。

## 有給休暇の申請（架空）
- 取得の3営業日前までに申請する。
- 連続5日以上の取得は上長の承認が必要。

`ENABLE_RAG=1` で起動し、`refresh_database()` 実行後に
「経費の申請期限は？」「有給は何日前に申請？」などと聞くと、
このファイルがヒットするはずです。
